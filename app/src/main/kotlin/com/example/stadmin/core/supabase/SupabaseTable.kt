package com.example.stadmin.core.supabase

enum class SupabaseTable(val value: String) {
    DEVICES("devices"),
    UPDATES("updates"),
    TRACES("traces"),
    TRACE_TRANSLATIONS("trace_translations"),
}